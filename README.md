# Simple Feedback for Laravel

## Installation

Install package using Composer:

```bash
composer require zeddmaster/laravel-feedback
```

Publish configs:
```bash
php artisan vendor:publish --provider="Zeddmaster\LaravelFeedback\Providers\FeedbackServiceProvider"
```